#include<stdio.h>
int main()
{
    int ac=12;
    float bc,ab,area;
    printf("Enter the number of ab=");
    scanf("%f", & ab);
    bc = sqrt((ac*ac)-(ab*ab));
    printf("The value of bc=%f", bc);
    area = ((ab*bc)/2);
    printf("Area of Triangle=%f", area);
    return 0;
}
